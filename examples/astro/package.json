{
  "name": "@saeris/hanko-example-astro",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "portless": {
    "name": "hanko",
    "script": "dev"
  },
  "scripts": {
    "dev": "astro dev",
    "share": "node scripts/share.mjs",
    "build": "astro build",
    "preview": "astro preview"
  },
  "_comment": "`link:` rather than `workspace:*` because the library IS the repo root. Yarn does not symlink a workspace root into node_modules — it is the project, not a dependency of itself — and without a symlink the bundler cannot resolve subpath exports like `@saeris/hanko/handlers`. The link restores normal `exports` resolution, so this demo consumes the published surface rather than reaching into src/.",
  "dependencies": {
    "@saeris/hanko": "link:../..",
    "astro": "~7.2.2",
    "qr-scanner": "1.4.2"
  },
  "devDependencies": {
    "@astrojs/node": "^11.1.0",
    "@types/node": "^25.9.1",
    "portless": "^0.15.6"
  }
}
